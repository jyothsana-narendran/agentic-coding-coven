# Public/browser-safe values only. Never put AWS credentials here.
VITE_API_URL=http://localhost:8000
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
# Use "api" for the real FastAPI/Bedrock pipeline or "mock" for local fixtures.
VITE_DATA_SOURCE=mock
VITE_API_URL=http://localhost:8000

# Development identity only. The current backend accepts this through X-User-ID.
VITE_DEV_USER_ID=career-craft-local-demo
